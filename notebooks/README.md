# notebooks/

Exploração narrada (EDA, validação de hipóteses, protótipos antes de virar código em `src/`).
Os notebooks contam a história da análise; o código de produção mora em `src/`.

> Os scripts do spike de viabilidade (validação inicial das fontes) viram a base aqui.
