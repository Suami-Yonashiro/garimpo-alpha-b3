# flows/

Orquestração com **Prefect** — os DAGs que agendam e encadeiam o pipeline
(extração → Bronze → Silver → Gold), com retries e agendamento (diário/trimestral).

> Vazio por enquanto. Preenchido na Fase 1 do roadmap (PRD §11).
